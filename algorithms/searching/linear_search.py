"""
🟢 LINEAR SEARCH (Линейный поиск)

Временная сложность:
- Лучший случай: O(1) - элемент найден на первой позиции
- Средний случай: O(n) - элемент найден в середине
- Худший случай: O(n) - элемент не найден или находится в конце

Пространственная сложность: O(1) - не требует дополнительной памяти

Особенности:
- Самый простой алгоритм поиска
- Работает на любых данных (отсортированных и неотсортированных)
- Не требует предварительной подготовки данных
- Гарантированно найдет элемент, если он существует
"""


def linear_search(arr, target):
    """
    🟢 Junior level
    Линейный поиск - проверяет каждый элемент по очереди
    
    Принцип работы:
    1. Начинаем с первого элемента
    2. Сравниваем с искомым элементом
    3. Если найден - возвращаем индекс
    4. Если нет - переходим к следующему
    5. Повторяем до конца массива
    
    Args:
        arr: массив для поиска
        target: искомый элемент
        
    Returns:
        индекс элемента или -1 если не найден
        
    >>> linear_search([1, 3, 5, 7, 9], 5)
    2
    >>> linear_search([1, 3, 5, 7, 9], 6)
    -1
    """
    for i, val in enumerate(arr):
        if val == target:
            return i
    return -1


# ============================================================================
# ПОШАГОВОЕ ИСПОЛНЕНИЕ
# ============================================================================

def linear_search_with_steps(arr, target):
    """
    Версия с выводом пошагового исполнения
    """
    print(f"Ищем элемент {target} в массиве {arr}")
    print("=" * 50)
    
    for i, val in enumerate(arr):
        print(f"Шаг {i + 1}: Проверяем arr[{i}] = {val}")
        if val == target:
            print(f"✓ Найден! Элемент {target} находится на позиции {i}")
            return i
        else:
            print(f"  {val} ≠ {target}, продолжаем поиск")
    
    print(f"✗ Элемент {target} не найден в массиве")
    return -1


# ============================================================================
# РАСШИРЕННЫЕ ВЕРСИИ
# ============================================================================

def linear_search_all_occurrences(arr, target):
    """
    Находит все вхождения элемента
    
    >>> linear_search_all_occurrences([1, 3, 5, 3, 7, 3], 3)
    [1, 3, 5]
    """
    indices = []
    for i, val in enumerate(arr):
        if val == target:
            indices.append(i)
    return indices


def linear_search_with_sentinel(arr, target):
    """
    Версия с барьером (sentinel) - добавляем искомый элемент в конец
    Это позволяет избежать проверки границ массива в цикле
    """
    # Создаем копию и добавляем sentinel
    arr_copy = arr.copy() + [target]
    
    i = 0
    while arr_copy[i] != target:
        i += 1
    
    # Возвращаем -1 если дошли до sentinel, иначе индекс
    return -1 if i == len(arr) else i


def linear_search_recursive(arr, target, index=0):
    """
    Рекурсивная версия линейного поиска
    """
    # Базовый случай
    if index >= len(arr):
        return -1
    
    # Найден элемент
    if arr[index] == target:
        return index
    
    # Рекурсивный вызов
    return linear_search_recursive(arr, target, index + 1)


# ============================================================================
# ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
# ============================================================================

def demonstrate_linear_search():
    """
    Демонстрация работы алгоритма
    """
    print("=" * 60)
    print("LINEAR SEARCH - Демонстрация")
    print("=" * 60)
    
    # Тест 1: Элемент найден
    test_arr1 = [1, 3, 5, 7, 9, 11, 13]
    target1 = 7
    print(f"\nТест 1: Ищем {target1} в {test_arr1}")
    result1 = linear_search(test_arr1, target1)
    print(f"Результат: индекс {result1}")
    
    # Тест 2: Элемент не найден
    test_arr2 = [1, 3, 5, 7, 9, 11, 13]
    target2 = 6
    print(f"\nТест 2: Ищем {target2} в {test_arr2}")
    result2 = linear_search(test_arr2, target2)
    print(f"Результат: {result2} (не найден)")
    
    # Тест 3: Элемент в начале
    test_arr3 = [5, 3, 8, 1, 9]
    target3 = 5
    print(f"\nТест 3: Ищем {target3} в {test_arr3}")
    result3 = linear_search(test_arr3, target3)
    print(f"Результат: индекс {result3}")
    
    # Тест 4: Элемент в конце
    test_arr4 = [5, 3, 8, 1, 9]
    target4 = 9
    print(f"\nТест 4: Ищем {target4} в {test_arr4}")
    result4 = linear_search(test_arr4, target4)
    print(f"Результат: индекс {result4}")
    
    # Пошаговое исполнение
    print("\n" + "=" * 60)
    print("ПОШАГОВОЕ ИСПОЛНЕНИЕ")
    print("=" * 60)
    linear_search_with_steps([5, 2, 8, 1, 9], 8)
    
    # Поиск всех вхождений
    print("\n" + "=" * 60)
    print("ПОИСК ВСЕХ ВХОЖДЕНИЙ")
    print("=" * 60)
    
    test_arr5 = [1, 3, 5, 3, 7, 3, 9]
    target5 = 3
    print(f"Ищем все вхождения {target5} в {test_arr5}")
    result5 = linear_search_all_occurrences(test_arr5, target5)
    print(f"Найдены на позициях: {result5}")
    
    # Сравнение версий
    print("\n" + "=" * 60)
    print("СРАВНЕНИЕ ВЕРСИЙ")
    print("=" * 60)
    
    test_arr = [1, 3, 5, 7, 9, 11]
    target = 7
    print(f"Массив: {test_arr}, ищем: {target}")
    print(f"Обычная версия: {linear_search(test_arr, target)}")
    print(f"С sentinel: {linear_search_with_sentinel(test_arr, target)}")
    print(f"Рекурсивная: {linear_search_recursive(test_arr, target)}")


# ============================================================================
# АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ
# ============================================================================

def performance_analysis():
    """
    Анализ производительности на разных типах данных
    """
    import time
    import random
    
    print("\n" + "=" * 60)
    print("АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ")
    print("=" * 60)
    
    sizes = [1000, 10000, 100000]
    
    for size in sizes:
        # Генерируем случайный массив
        arr = [random.randint(1, size * 2) for _ in range(size)]
        target = random.choice(arr)  # Гарантированно существующий элемент
        
        # Тестируем производительность
        start = time.time()
        result = linear_search(arr, target)
        search_time = time.time() - start
        
        print(f"\nРазмер массива: {size}")
        print(f"Элемент найден на позиции: {result}")
        print(f"Время поиска: {search_time*1000:.3f}ms")
        print(f"Количество сравнений: {result + 1}")
    
    # Сравнение с бинарным поиском на отсортированном массиве
    print("\n" + "=" * 40)
    print("СРАВНЕНИЕ С БИНАРНЫМ ПОИСКОМ")
    print("=" * 40)
    
    sorted_arr = list(range(1000))
    target = 500
    
    # Линейный поиск
    start = time.time()
    linear_result = linear_search(sorted_arr, target)
    linear_time = time.time() - start
    
    # Бинарный поиск (имитация)
    start = time.time()
    binary_result = sorted_arr.index(target)  # Встроенный метод
    binary_time = time.time() - start
    
    print(f"Отсортированный массив размером 1000, ищем элемент 500:")
    print(f"Линейный поиск: {linear_time*1000:.3f}ms, {linear_result + 1} сравнений")
    print(f"Бинарный поиск: {binary_time*1000:.3f}ms, ~{10} сравнений (log₂(1000))")


# ============================================================================
# ПРАКТИЧЕСКИЕ ПРИМЕНЕНИЯ
# ============================================================================

def practical_applications():
    """
    Практические применения линейного поиска
    """
    print("\n" + "=" * 60)
    print("ПРАКТИЧЕСКИЕ ПРИМЕНЕНИЯ")
    print("=" * 60)
    
    print("1. Поиск в неотсортированных данных:")
    print("   - Единственный способ поиска в произвольном массиве")
    print("   - Поиск в списках, которые часто изменяются")
    
    print("\n2. Малые массивы:")
    print("   - Для n < 50 линейный поиск может быть быстрее бинарного")
    print("   - Меньше накладных расходов")
    
    print("\n3. Поиск в связанных списках:")
    print("   - Единственный способ поиска в linked list")
    print("   - Нельзя применить бинарный поиск без индексации")
    
    print("\n4. Поиск в файлах:")
    print("   - Последовательное чтение файла")
    print("   - Поиск подстроки в тексте")
    
    print("\n5. Отладка и тестирование:")
    print("   - Простота реализации")
    print("   - Легко понять и отладить")
    
    print("\n6. Онлайн алгоритмы:")
    print("   - Поиск в потоке данных")
    print("   - Не требует знания всех данных заранее")


# ============================================================================
# ОПТИМИЗАЦИИ И МОДИФИКАЦИИ
# ============================================================================

def optimizations_examples():
    """
    Примеры оптимизаций и модификаций
    """
    print("\n" + "=" * 60)
    print("ОПТИМИЗАЦИИ И МОДИФИКАЦИИ")
    print("=" * 60)
    
    print("1. Ранний выход:")
    print("   - Если элемент найден, сразу возвращаем результат")
    print("   - Не продолжаем поиск после находки")
    
    print("\n2. Поиск с барьером (Sentinel):")
    print("   - Добавляем искомый элемент в конец")
    print("   - Убираем проверку границ в цикле")
    
    print("\n3. Поиск всех вхождений:")
    print("   - Продолжаем поиск после первой находки")
    print("   - Собираем все индексы")
    
    print("\n4. Поиск с условием:")
    print("   - Не просто равенство, а любое условие")
    print("   - Поиск первого четного, первого больше 100, и т.д.")
    
    print("\n5. Поиск в обратном порядке:")
    print("   - Начинаем с конца массива")
    print("   - Полезно для поиска последнего вхождения")


# ============================================================================
# СРАВНЕНИЕ С ДРУГИМИ АЛГОРИТМАМИ
# ============================================================================

def comparison_with_other_algorithms():
    """
    Сравнение с другими алгоритмами поиска
    """
    print("\n" + "=" * 60)
    print("СРАВНЕНИЕ С ДРУГИМИ АЛГОРИТМАМИ")
    print("=" * 60)
    
    print("Линейный поиск vs Бинарный поиск:")
    print("┌─────────────────┬─────────────────┬─────────────────┐")
    print("│ Характеристика  │ Линейный поиск  │ Бинарный поиск  │")
    print("├─────────────────┼─────────────────┼─────────────────┤")
    print("│ Временная сложн.│ O(n)            │ O(log n)        │")
    print("│ Требования      │ Любые данные    │ Отсортированные │")
    print("│ Доп. память     │ O(1)            │ O(1)            │")
    print("│ Реализация      │ Простая         │ Средняя         │")
    print("│ Скорость (n=1000│ ~500 сравнений  │ ~10 сравнений   │")
    print("└─────────────────┴─────────────────┴─────────────────┘")
    
    print("\nКогда использовать линейный поиск:")
    print("✓ Неотсортированные данные")
    print("✓ Малые массивы (< 50 элементов)")
    print("✓ Редкие поиски")
    print("✓ Простота реализации важнее скорости")
    
    print("\nКогда НЕ использовать линейный поиск:")
    print("✗ Отсортированные данные (используйте бинарный)")
    print("✗ Частые поиски в больших массивах")
    print("✗ Критична производительность")


if __name__ == "__main__":
    demonstrate_linear_search()
    performance_analysis()
    practical_applications()
    optimizations_examples()
    comparison_with_other_algorithms()