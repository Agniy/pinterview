"""
🟢 SELECTION SORT (Сортировка выбором)

Временная сложность:
- Лучший случай: O(n²)
- Средний случай: O(n²)
- Худший случай: O(n²)

Пространственная сложность: O(1) - in-place алгоритм

Стабильность: Нет (может изменить порядок равных элементов)
"""


def selection_sort(arr):
    """
    🟢 Junior level
    Сортировка выбором - на каждом шаге выбирает минимальный элемент
    
    Принцип работы:
    1. Находим минимальный элемент в неотсортированной части
    2. Меняем его местами с первым элементом неотсортированной части
    3. Повторяем для оставшейся части массива
    
    Args:
        arr: список для сортировки
        
    Returns:
        отсортированный список
        
    >>> selection_sort([64, 25, 12, 22, 11])
    [11, 12, 22, 25, 64]
    """
    arr = arr.copy()
    n = len(arr)
    
    # Внешний цикл - проходим по всем позициям
    for i in range(n):
        # Находим индекс минимального элемента в оставшейся части
        min_idx = i
        for j in range(i + 1, n):
            if arr[j] < arr[min_idx]:
                min_idx = j
        
        # Меняем минимальный элемент с элементом в позиции i
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    
    return arr


# ============================================================================
# ПОШАГОВОЕ ИСПОЛНЕНИЕ
# ============================================================================

def selection_sort_with_steps(arr):
    """
    Версия с выводом пошагового исполнения
    """
    arr = arr.copy()
    n = len(arr)
    
    print(f"Исходный массив: {arr}")
    print(f"Длина массива: {n}")
    print("=" * 50)
    
    for i in range(n):
        print(f"\nИтерация {i + 1}:")
        print(f"  Ищем минимум в части [{i}:{n}] = {arr[i:]}")
        
        # Находим минимальный элемент
        min_idx = i
        for j in range(i + 1, n):
            print(f"    Сравниваем arr[{j}]={arr[j]} с текущим минимумом arr[{min_idx}]={arr[min_idx]}")
            if arr[j] < arr[min_idx]:
                min_idx = j
                print(f"    Новый минимум: arr[{min_idx}]={arr[min_idx]}")
        
        print(f"  Найден минимум: arr[{min_idx}]={arr[min_idx]}")
        
        # Меняем местами
        if min_idx != i:
            print(f"  Меняем местами arr[{i}]={arr[i]} и arr[{min_idx}]={arr[min_idx]}")
            arr[i], arr[min_idx] = arr[min_idx], arr[i]
            print(f"  Массив после обмена: {arr}")
        else:
            print(f"  Элемент уже на своем месте, обмен не нужен")
        
        print(f"  Результат итерации {i + 1}: {arr}")
    
    print(f"\nИтоговый результат: {arr}")
    return arr


# ============================================================================
# ОПТИМИЗИРОВАННАЯ ВЕРСИЯ
# ============================================================================

def selection_sort_optimized(arr):
    """
    Оптимизированная версия - находит и минимум, и максимум одновременно
    """
    arr = arr.copy()
    n = len(arr)
    
    for i in range(n // 2):
        min_idx = i
        max_idx = n - 1 - i
        
        # Находим минимум и максимум в оставшейся части
        for j in range(i, n - i):
            if arr[j] < arr[min_idx]:
                min_idx = j
            if arr[j] > arr[max_idx]:
                max_idx = j
        
        # Меняем минимум с началом
        if min_idx != i:
            arr[i], arr[min_idx] = arr[min_idx], arr[i]
        
        # Меняем максимум с концом (если он не был перемещен)
        if max_idx == i:
            max_idx = min_idx
        if max_idx != n - 1 - i:
            arr[n - 1 - i], arr[max_idx] = arr[max_idx], arr[n - 1 - i]
    
    return arr


# ============================================================================
# ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
# ============================================================================

def demonstrate_selection_sort():
    """
    Демонстрация работы алгоритма
    """
    print("=" * 60)
    print("SELECTION SORT - Демонстрация")
    print("=" * 60)
    
    # Тест 1: Простой массив
    test_arr1 = [64, 25, 12, 22, 11]
    print(f"\nТест 1: {test_arr1}")
    result1 = selection_sort(test_arr1)
    print(f"Результат: {result1}")
    
    # Тест 2: Уже отсортированный массив
    test_arr2 = [1, 2, 3, 4, 5]
    print(f"\nТест 2 (уже отсортирован): {test_arr2}")
    result2 = selection_sort(test_arr2)
    print(f"Результат: {result2}")
    
    # Тест 3: Обратно отсортированный массив
    test_arr3 = [5, 4, 3, 2, 1]
    print(f"\nТест 3 (обратный порядок): {test_arr3}")
    result3 = selection_sort(test_arr3)
    print(f"Результат: {result3}")
    
    # Тест 4: Массив с дубликатами
    test_arr4 = [3, 1, 4, 1, 5, 9, 2, 6, 5]
    print(f"\nТест 4 (с дубликатами): {test_arr4}")
    result4 = selection_sort(test_arr4)
    print(f"Результат: {result4}")
    
    # Пошаговое исполнение
    print("\n" + "=" * 60)
    print("ПОШАГОВОЕ ИСПОЛНЕНИЕ на примере [5, 2, 8, 1, 9]")
    print("=" * 60)
    selection_sort_with_steps([5, 2, 8, 1, 9])
    
    # Сравнение с оптимизированной версией
    print("\n" + "=" * 60)
    print("СРАВНЕНИЕ С ОПТИМИЗИРОВАННОЙ ВЕРСИЕЙ")
    print("=" * 60)
    
    test_arr = [64, 34, 25, 12, 22, 11, 90]
    print(f"Исходный массив: {test_arr}")
    print(f"Обычная версия: {selection_sort(test_arr)}")
    print(f"Оптимизированная: {selection_sort_optimized(test_arr)}")


# ============================================================================
# АНАЛИЗ СЛОЖНОСТИ
# ============================================================================

def analyze_complexity():
    """
    Анализ временной сложности
    """
    print("\n" + "=" * 60)
    print("АНАЛИЗ ВРЕМЕННОЙ СЛОЖНОСТИ")
    print("=" * 60)
    
    print("Selection Sort всегда выполняет O(n²) сравнений:")
    print("- Внешний цикл: n итераций")
    print("- Внутренний цикл: в среднем n/2 итераций на каждой внешней")
    print("- Общее количество сравнений: n × (n-1) / 2 = O(n²)")
    print()
    print("Количество обменов: O(n) - максимум один обмен на итерацию")
    print()
    print("Преимущества:")
    print("- Простота реализации")
    print("- Минимальное количество обменов")
    print("- In-place алгоритм")
    print()
    print("Недостатки:")
    print("- Всегда O(n²) сравнений")
    print("- Нестабильный")
    print("- Медленный для больших массивов")


if __name__ == "__main__":
    demonstrate_selection_sort()
    analyze_complexity()
