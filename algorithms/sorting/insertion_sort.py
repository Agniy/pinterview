"""
🟢 INSERTION SORT (Сортировка вставками)

Временная сложность:
- Лучший случай: O(n) - когда массив уже отсортирован
- Средний случай: O(n²)
- Худший случай: O(n²) - когда массив отсортирован в обратном порядке

Пространственная сложность: O(1) - in-place алгоритм

Стабильность: Да (сохраняет порядок равных элементов)

Особенности:
- Эффективен для небольших массивов
- Адаптивный алгоритм (быстро работает на почти отсортированных данных)
- Используется как часть других алгоритмов (Timsort, Quick Sort)
"""


def insertion_sort(arr):
    """
    🟢 Junior level
    Сортировка вставками - вставляет элемент в правильную позицию
    
    Принцип работы:
    1. Берем элемент из неотсортированной части
    2. Сравниваем его с элементами отсортированной части
    3. Вставляем в правильную позицию, сдвигая остальные элементы
    
    Args:
        arr: список для сортировки
        
    Returns:
        отсортированный список
        
    >>> insertion_sort([12, 11, 13, 5, 6])
    [5, 6, 11, 12, 13]
    """
    arr = arr.copy()
    
    # Начинаем со второго элемента (индекс 1)
    for i in range(1, len(arr)):
        key = arr[i]  # Текущий элемент для вставки
        j = i - 1     # Индекс последнего элемента отсортированной части
        
        # Сдвигаем элементы больше key вправо
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        
        # Вставляем key в правильную позицию
        arr[j + 1] = key
    
    return arr


# ============================================================================
# ПОШАГОВОЕ ИСПОЛНЕНИЕ
# ============================================================================

def insertion_sort_with_steps(arr):
    """
    Версия с выводом пошагового исполнения
    """
    arr = arr.copy()
    n = len(arr)
    
    print(f"Исходный массив: {arr}")
    print(f"Длина массива: {n}")
    print("=" * 50)
    
    for i in range(1, n):
        key = arr[i]
        print(f"\nИтерация {i}:")
        print(f"  Вставляем элемент arr[{i}]={key}")
        print(f"  Отсортированная часть: {arr[:i]}")
        print(f"  Неотсортированная часть: {arr[i:]}")
        
        j = i - 1
        shifts = 0
        
        print(f"  Начинаем поиск позиции для {key}:")
        
        # Сдвигаем элементы
        while j >= 0 and arr[j] > key:
            print(f"    arr[{j}]={arr[j]} > {key}, сдвигаем вправо")
            arr[j + 1] = arr[j]
            j -= 1
            shifts += 1
            print(f"    Промежуточный результат: {arr}")
        
        if shifts > 0:
            print(f"    Сделано {shifts} сдвигов")
        else:
            print(f"    Сдвигов не требуется")
        
        # Вставляем элемент
        arr[j + 1] = key
        print(f"  Вставляем {key} в позицию {j + 1}")
        print(f"  Результат итерации: {arr}")
    
    print(f"\nИтоговый результат: {arr}")
    return arr


# ============================================================================
# РЕКУРСИВНАЯ ВЕРСИЯ
# ============================================================================

def insertion_sort_recursive(arr, n=None):
    """
    Рекурсивная версия сортировки вставками
    """
    if n is None:
        n = len(arr)
    
    # Базовый случай
    if n <= 1:
        return arr
    
    # Рекурсивно сортируем первые n-1 элементов
    insertion_sort_recursive(arr, n - 1)
    
    # Вставляем последний элемент в правильную позицию
    last = arr[n - 1]
    j = n - 2
    
    # Сдвигаем элементы больше last вправо
    while j >= 0 and arr[j] > last:
        arr[j + 1] = arr[j]
        j -= 1
    
    arr[j + 1] = last
    
    return arr


# ============================================================================
# БИНАРНАЯ ВЕРСИЯ
# ============================================================================

def binary_insertion_sort(arr):
    """
    Версия с бинарным поиском позиции для вставки
    Временная сложность: O(n²) сравнений, но меньше сдвигов
    """
    arr = arr.copy()
    
    for i in range(1, len(arr)):
        key = arr[i]
        
        # Используем бинарный поиск для нахождения позиции
        pos = binary_search_position(arr, key, 0, i - 1)
        
        # Сдвигаем элементы
        for j in range(i, pos, -1):
            arr[j] = arr[j - 1]
        
        # Вставляем элемент
        arr[pos] = key
    
    return arr


def binary_search_position(arr, key, left, right):
    """
    Бинарный поиск позиции для вставки
    """
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] <= key:
            left = mid + 1
        else:
            right = mid - 1
    
    return left


# ============================================================================
# ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
# ============================================================================

def demonstrate_insertion_sort():
    """
    Демонстрация работы алгоритма
    """
    print("=" * 60)
    print("INSERTION SORT - Демонстрация")
    print("=" * 60)
    
    # Тест 1: Простой массив
    test_arr1 = [12, 11, 13, 5, 6]
    print(f"\nТест 1: {test_arr1}")
    result1 = insertion_sort(test_arr1)
    print(f"Результат: {result1}")
    
    # Тест 2: Уже отсортированный массив
    test_arr2 = [1, 2, 3, 4, 5]
    print(f"\nТест 2 (уже отсортирован): {test_arr2}")
    result2 = insertion_sort(test_arr2)
    print(f"Результат: {result2}")
    
    # Тест 3: Обратно отсортированный массив
    test_arr3 = [5, 4, 3, 2, 1]
    print(f"\nТест 3 (обратный порядок): {test_arr3}")
    result3 = insertion_sort(test_arr3)
    print(f"Результат: {result3}")
    
    # Тест 4: Почти отсортированный массив
    test_arr4 = [1, 2, 4, 3, 5, 6, 8, 7, 9]
    print(f"\nТест 4 (почти отсортирован): {test_arr4}")
    result4 = insertion_sort(test_arr4)
    print(f"Результат: {result4}")
    
    # Пошаговое исполнение
    print("\n" + "=" * 60)
    print("ПОШАГОВОЕ ИСПОЛНЕНИЕ на примере [5, 2, 8, 1, 9]")
    print("=" * 60)
    insertion_sort_with_steps([5, 2, 8, 1, 9])
    
    # Сравнение версий
    print("\n" + "=" * 60)
    print("СРАВНЕНИЕ ВЕРСИЙ")
    print("=" * 60)
    
    test_arr = [64, 34, 25, 12, 22, 11, 90]
    print(f"Исходный массив: {test_arr}")
    print(f"Обычная версия: {insertion_sort(test_arr)}")
    print(f"Рекурсивная версия: {insertion_sort_recursive(test_arr.copy())}")
    print(f"Бинарная версия: {binary_insertion_sort(test_arr)}")


# ============================================================================
# АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ
# ============================================================================

def performance_analysis():
    """
    Анализ производительности на разных типах данных
    """
    import time
    import random
    
    print("\n" + "=" * 60)
    print("АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ")
    print("=" * 60)
    
    sizes = [100, 500, 1000]
    
    for size in sizes:
        print(f"\nРазмер массива: {size}")
        
        # Случайный массив
        random_arr = [random.randint(1, 1000) for _ in range(size)]
        start = time.time()
        insertion_sort(random_arr.copy())
        random_time = time.time() - start
        
        # Отсортированный массив
        sorted_arr = list(range(size))
        start = time.time()
        insertion_sort(sorted_arr.copy())
        sorted_time = time.time() - start
        
        # Обратно отсортированный массив
        reverse_arr = list(range(size, 0, -1))
        start = time.time()
        insertion_sort(reverse_arr.copy())
        reverse_time = time.time() - start
        
        print(f"  Случайный массив: {random_time*1000:.2f}ms")
        print(f"  Отсортированный:  {sorted_time*1000:.2f}ms")
        print(f"  Обратный порядок: {reverse_time*1000:.2f}ms")
        print(f"  Отношение (случайный/отсортированный): {random_time/sorted_time:.1f}x")


# ============================================================================
# ПРАКТИЧЕСКИЕ ПРИМЕНЕНИЯ
# ============================================================================

def practical_examples():
    """
    Практические примеры использования
    """
    print("\n" + "=" * 60)
    print("ПРАКТИЧЕСКИЕ ПРИМЕНЕНИЯ")
    print("=" * 60)
    
    print("1. Сортировка небольших массивов:")
    print("   - Эффективен для n < 50")
    print("   - Простая реализация")
    print("   - Стабильная сортировка")
    
    print("\n2. Часть других алгоритмов:")
    print("   - Timsort использует Insertion Sort для небольших частей")
    print("   - Quick Sort использует Insertion Sort для малых подмассивов")
    print("   - Hybrid сортировки")
    
    print("\n3. Онлайн алгоритмы:")
    print("   - Можно сортировать данные по мере поступления")
    print("   - Не требует знания всего массива заранее")
    
    print("\n4. Почти отсортированные данные:")
    print("   - Очень эффективен на частично отсортированных массивах")
    print("   - Адаптивный алгоритм")


if __name__ == "__main__":
    demonstrate_insertion_sort()
    performance_analysis()
    practical_examples()
